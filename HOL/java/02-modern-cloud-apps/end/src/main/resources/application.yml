application:
  incidentApiUrl:
  incidentResourcePath:
  redisHost:
  redisPort:
  primaryKey:
  redisSslPort:

azure:
  storage:
    account:
      name:
      key:
      blobContainer:
      queue:

logging:
  level: info

spring:
  http:
    multipart:
      enabled: true
      max-file-size: 3mb
      max-request-size: 20mb
  application:
    name: DevCamp demo App
  profiles:
    active: default

server:
  port: 8083




